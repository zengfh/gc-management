export default function App() {
  return (
    <main className="app-shell">
      <section className="boot-panel" aria-labelledby="app-title">
        <p className="eyebrow">Implementation baseline</p>
        <h1 id="app-title">Secure Gift Card Manager</h1>
        <p>
          Fresh React 19 and Node/Express project setup is ready. Core product
          implementation will follow the contracts in <code>/docs</code>.
        </p>
      </section>
    </main>
  );
}
