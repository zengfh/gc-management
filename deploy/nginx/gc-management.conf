# Replace giftcards.example.com and certificate paths before enabling this file.
server {
  listen 80;
  server_name giftcards.example.com;

  return 301 https://$host$request_uri;
}

server {
  listen 443 ssl http2;
  server_name giftcards.example.com;

  ssl_certificate /etc/letsencrypt/live/giftcards.example.com/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/giftcards.example.com/privkey.pem;

  client_max_body_size 2m;

  add_header X-Content-Type-Options nosniff always;
  add_header Referrer-Policy no-referrer always;

  location / {
    proxy_pass http://127.0.0.1:3001;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Host $host;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
  }
}
